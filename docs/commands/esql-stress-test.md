# `esql-stress-test`

Run several ESQL queries in parallel to stress Elasticsearch.

## Usage

```bash
yarn start esql-stress-test [-p <parallel>]
```

## Options

- `-p <parallel>`: Number of parallel runs (default: `1`)
