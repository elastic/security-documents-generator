# `entity-store`

Interactive Entity Store generation flow.

## Usage

```bash
yarn start entity-store [--space <space>]
```

## What it prompts for

- Enabled generation options (seed, criticality, risk engine, rule, agents, API enrichment)
- Entity counts (users, hosts, services, generic entities)
- Event offset hours
- Seed value (optional)
