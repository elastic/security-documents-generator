# `generate-events`

Generate synthetic events into the configured event index.

## Usage

```bash
yarn start generate-events <count>
```

## Arguments

- `<count>`: Number of events to generate

## Notes

- Events are written to `eventIndex` from config (or `EVENT_INDEX` env var).
